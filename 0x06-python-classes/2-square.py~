#!/usr/bin/python3
"""Defining a class Square"""


class Square:
    """A class with size as a private instance attribute"""
    def __init__(self, size=0):
        if not isinstance(int, size):
            raise TypeError("size must be an integer")
        elif size < 0:
            raise ValueError("size must be >= 0")
        else:
            self.__size = size
